language: python
python:
  - 3.6
  - 2.7
git:
  depth: 3
install:
  - pip install -e ./src/
  - pip install -r requirements.txt
env:
  - PYLINT_TARGET=./src/checkers
  - PYLINT_TARGET=./src/sfcli
  - UNIT_TEST=true
script: scripts/verify.sh
